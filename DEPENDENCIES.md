# Dependency ledger

Every dependency answers three questions before it enters (STYLE §Dependencies,
`.agents/skills/crate-boundaries`): what breaks when it breaks, what is the
eject path, and whether roughly 50 lines of ours could replace it. The
`check-discipline` binary compares these rows exactly with Cargo metadata and
every workspace `package.json`; missing, stale, duplicate, or incomplete rows
fail `just ci`.

## Rust (crates.io)

| Dependency | Used by | Failure surface | Eject path | Why ours cannot replace it | Owner |
|---|---|---|---|---|---|
| `argon2` | `bins/pos-server` auth v0 password hashing | Signup/login stop closed; no project-data path is affected | The §20 auth decision (own argon2id + sessions); PHC strings record their parameters, so a KDF migration verifies old hashes while minting new ones | A memory-hard KDF is security-critical primitive code; hand-rolling one is the canonical crypto mistake | founders |
| `axum` | `pos-api` HTTP+SSE transport (`http` feature), served by `bins/pos-server` | The web shell cannot serve the API; desktop IPC and the CLI are unaffected | The §20 web-server row is deliberately one-way-door-free: the transport is thin dispatch behind `pos-api`, so swapping servers rewrites one module and zero registry code | Correct HTTP/1.1 parsing, routing, and body-limit enforcement are security-critical commodity infrastructure, not 50 local lines | founders |
| `getrandom` | `bins/pos-server` session tokens, salts, and ids; `pos-sched` retry-jitter seeding | New logins stop closed (existing sessions keep verifying); no project-data path is affected | The thinnest possible seam over the OS entropy syscall; replaceable by any maintained binding in one helper function | OS-specific entropy plumbing (getrandom(2), arc4random, BCryptGenRandom) is exactly the code nobody should write twice | founders |
| `blake3` | `pos-store` blob CAS; `pos-gateway` prompt-lock pins and model-artifact verification (m0-s11); `pos-api` test fixtures | Content addresses cannot be computed or verified; blob reads/writes stop closed | The hash is the on-disk address format (§7.2), so replacing it is a format-version migration behind `BlobHash` | BLAKE3 is a §20-adjacent format decision: a vetted, SIMD-accelerated cryptographic hash is not 50 replaceable lines | founders |
| `ciborium` | `pos-log` refs/snapshots, `pos-domain` event bodies | Event bodies cannot encode or decode; append and replay stop closed | CBOR is the §7.1 body format; the codec hides behind encode/decode seams, so swapping the implementation is bounded — leaving CBOR is a format-version migration | A correct, fuzz-hardened RFC 8949 codec is safety-critical parsing, not 50 local lines; ciborium is the maintained serde implementation | founders |
| `jiff` | `pos-sched` cron evaluation (m0-s14) | Cron schedules cannot resolve a local time to an instant, so the tick refuses typed and no job fires; the queue itself is unaffected | Ours is one `CronSchedule` seam over `TimeZone::to_ambiguous_zoned`; ejecting means a TZif v2 parser plus civil arithmetic (~600 lines) behind that same seam | A cron fires in civil time, and twice a year that civil time either does not exist or exists twice; getting the IANA rules and the gap/ambiguity policy right is exactly the correctness we must not improvise, and the zone database must track OS updates | founders |
| `clap` | `bins/pos` argument parsing | The CLI cannot parse its subcommands; the API and shells are unaffected | Replace with hand-rolled argument matching behind the one `Cli` type; bounded because the CLI is thin dispatch | Correct help/error UX, long-form flags, and completion-ready parsing are commodity CLI infrastructure the ecosystem standardizes on | founders |
| `proptest` | dev-only: pos-log/pos-store property suites and pos-agents resume oracle | The §18 replay-determinism, CAS, and Run resume-correctness property oracles stop running; no production code path is affected | Replace with another property framework or exhaustive fixtures; the properties themselves are ours | Shrinking, seeded RNG strategies, and failure persistence are years of tooling, not 50 lines | founders |
| `proc-macro2` | `check-discipline` | Source spans become unavailable and discipline CI stops closed | Replace span lookup with a maintained lexer; roughly 100 lines plus adversarial tests | It supplies compiler-compatible token spans and is already in syn's supply chain | founders |
| `rusqlite` | `pos-store` engine (re-exported to `pos-log` only) | Every project database operation stops closed | SQLite is the §20 storage decision; the binding hides behind `ProjectDb`/`ProjectStore`, so replacing the wrapper is bounded — replacing SQLite itself is a milestone | Safe, maintained bindings over the bundled SQLite amalgamation with FTS5; hand-rolled FFI over sqlite3.h is exactly the unsafe surface STYLE minimizes | founders |
| `serde` | `pos-foundation` ids, event bodies, manifests; `pos-gateway` prompt/eval/manifest shapes | Typed payloads cannot serialize; log append/replay and manifest I/O stop closed | The trait boundary is the ecosystem standard; replacement means hand-written codecs per payload — a format-bearing migration | Versioned event bodies are serde structs by STYLE/master-plan decision; a bespoke derive framework is not product leverage | founders |
| `sqlite-vec` | `pos-store` vector index extension | The `vec0` module stops loading and vector projections cannot build (M1 search) | Extension loading is one audited leaf (`extensions.rs`); swap for another embedded vector index behind re-projection (indexes are rebuildable projections, L1) | An embedded SIMD vector-search virtual table is a specialized C artifact, not 50 local lines; §20 chose it over a vector-DB service | founders |
| `serde_json` | repository checkers; `pos-api` wire results; `pos-gateway` provider wire bodies and the model manifest | Cargo/npm metadata cannot be decoded and boundary CI stops closed | Replace metadata transport or write a bounded JSON decoder; at least several hundred security-sensitive lines | JSON correctness is commodity infrastructure and serde is the Rust ecosystem standard | founders |
| `syn` | `check-discipline` | Rust policy scanning cannot distinguish code, test items, macros, and literals, so discipline CI stops closed | Replace with rustc tooling or a maintained Rust lexer/parser; multiple engineer-days | A 50-line grep cannot parse Rust without false positives or bypasses | founders |
| `tauri` | `apps/desktop` | The native desktop window, webview, and event loop cannot boot | Replace the thin shell behind `pos-api`; domain crates and the shared UI remain unchanged, but native packaging is a planned migration | §20 chooses Tauri v2 for the small system-webview shell and Rust-core reuse; recreating a cross-platform shell is not product leverage | founders |
| `tauri-build` | `apps/desktop` build | Tauri configuration and capability metadata are not generated, so desktop compilation stops closed | Replace alongside Tauri or own equivalent platform build generation | It is the official build half of the selected Tauri v2 stack and must version with it | founders |
| `tauri-plugin-dialog` | `apps/desktop` native project picker | The native directory picker stops; the typed path field still creates and opens projects, so no capability is lost | Drop with any Tauri replacement, or call each platform's picker directly behind the same one-function seam | Native file dialogs are three per-OS APIs with their own threading rules; the official plugin versions with the shell that hosts them | founders |
| `tauri-plugin-single-instance` | `apps/desktop` launch | A second launch would open a second window against the same project directory, which is the single-writer violation this plugin exists to prevent | Replace with an OS-specific lock file plus a focus IPC; roughly 100 lines per platform with real edge cases (stale locks, crash recovery) | Per-platform instance detection and window focusing is exactly the boring cross-platform glue the Tauri stack is chosen to own | founders |
| `tempfile` | store/log/CLI/agent-harness test fixtures; `pos-api` ts-rs staleness check (scratch generation tree) | The generated-type staleness gate and isolated durability/resume fixtures stop working; no project-data path is affected | Replace with a small fixture helper over `std::env::temp_dir` plus cleanup | Correct cross-platform unique-dir creation with drop cleanup is subtle (races, leak-on-panic); the crate is the community answer | founders |
| `tokio` | `pos-api` `http` feature (blocking-pool dispatch), `bins/pos-server` (listener, signals, runtime), `pos-sched` worker pool (task supervision; all queue and handler work runs on `spawn_blocking`) | The web shell cannot run its event loop and background jobs stop being claimed; durable queue state, desktop IPC, and the CLI are unaffected | The §20 async-runtime row; usage is confined to the transport layer, the server binary, and `pos-sched::pool`, so a runtime swap never touches domain crates | A correct work-stealing async runtime with I/O drivers is a decade of infrastructure the §20 decision deliberately buys | founders |
| `tracing` | `pos-foundation` telemetry facade — the single emission point every ProjectOS span goes through (m0-s15) | Spans stop being emitted to any `tracing`-aware tool; the ProjectOS sink, the cost ledger, and every product path are unaffected | The §20 observability decision. `pos_foundation::telemetry` is the only caller and owns the typed span/field vocabulary, so replacing the emission layer is one module; the `check-discipline` telemetry rule keeps it that way | It is the Rust ecosystem's structured-diagnostics facade, and the M1 OTLP bridge plus any contributor's development subscriber attach through it — a private span format would make ProjectOS traces unreadable by every existing tool | founders |
| `tokio-stream` | `pos-api` HTTP/SSE transport (`http` feature) | Live HTTP Run feeds stop streaming; durable Run steps and desktop IPC remain available | Replace its one `ReceiverStream` adapter with another bounded `Stream` wrapper over Tokio's channel | The crate is the official, audited adapter between the selected Tokio runtime and the standard stream trait; owning a pin/waker adapter adds risk without product value | founders |
| `ts-rs` | `pos-api` TypeScript generation into `apps/ui/src/api/gen/api` | The UI type pipeline stops closed: types cannot regenerate and the staleness gate fails | The generated files are checked in, so the product keeps building; replacement is another Rust→TS generator behind the same `export-api-types` contract | Deriving TS from Rust types with serde-attribute fidelity is the exact drift-prone glue L12 forbids hand-maintaining | founders |

## npm (apps/ui)

| Dependency | Used by | Failure surface | Eject path | Why ours cannot replace it | Owner |
|---|---|---|---|---|---|
| `@tauri-apps/api` | `apps/ui` desktop transport adapter | Tauri IPC commands still exist, but the webview cannot construct the typed channel used by live Run feeds | Replace alongside Tauri or wrap the next shell's native channel behind `apiStream`; HTTP/SSE remains unchanged | The channel id, callback lifecycle, and native IPC framing are private shell protocol details maintained by the selected Tauri stack | founders |
| `react` | `apps/ui` runtime | The shared desktop/web component tree cannot render | Migrate behind component and hook seams; a planned UI-framework migration | §20 chooses React for ecosystem depth and hiring leverage; recreating a renderer is outside product scope | founders |
| `react-dom` | `apps/ui` runtime | The React tree cannot mount in browser or Tauri webview | Move the mount adapter with any React replacement | Browser reconciliation and event plumbing are framework infrastructure, not 50 lines | founders |
| `typescript` | `apps/ui` build | Generated API and strict UI type checks stop closed | Pin/upgrade the compiler or migrate the UI language as a one-way-door ADR | A type checker cannot be replaced locally; strict TypeScript is a §20 decision | founders |
| `vite` | `apps/ui` build | Development server and production UI bundle stop | Replace the build adapter with another ESM bundler and update packaging tests | Bundling, HMR, and asset graphs are commodity tooling far beyond 50 lines | founders |
| `@vitejs/plugin-react` | `apps/ui` build | React transforms and fast refresh stop | Replace alongside Vite or configure an equivalent React transform | Correct JSX transforms and refresh integration belong to the selected build stack | founders |
| `eslint` | `apps/ui` CI | Mechanical UI source rules stop closed | Move enforced rules to another TypeScript-aware linter | The rule engine and editor ecosystem are commodity tooling, not product code | founders |
| `typescript-eslint` | `apps/ui` CI | TypeScript AST lint rules, including no-any, stop | Replace with another TypeScript-aware ESLint bridge or linter | Parsing TypeScript correctly is not a safe local utility | founders |
| `prettier` | `apps/ui` CI | Deterministic UI formatting stops closed | Replace with a pinned formatter and one mechanical rewrite | A mature TS/TSX formatter avoids maintaining syntax-sensitive style code | founders |
| `@types/react` | `apps/ui` build | React APIs lose static types and strict compilation fails | Replace with types shipped by a future framework/runtime | These declarations track React's public contract and must version with it | founders |
| `@types/react-dom` | `apps/ui` build | DOM mounting APIs lose static types and strict compilation fails | Replace with types shipped by a future mount adapter | These declarations track react-dom and cannot be safely hand-maintained | founders |
| `@tauri-apps/cli` | desktop development and packaging | `tauri dev` and later bundle commands stop; browser UI development remains available | Use the pinned Rust CLI or replace alongside the Tauri shell | The official CLI validates config, coordinates the UI build, and drives native packaging; reproducing it is outside the thin-shell boundary | founders |
| `@playwright/test` | `apps/ui` walking-skeleton e2e | The `public-builds-alone` end-to-end evidence stops, so shell regressions reach a user before CI | Replace with another browser-automation runner and rewrite the specs; the assertions themselves are ours | Driving a real browser needs a maintained CDP client, a test runner, and browser downloads — none of which is 50 lines of ours | founders |
| `@types/node` | `apps/ui` e2e and build-config typechecking | The e2e spec and Playwright/Vite configs lose Node API types and strict compilation stops closed | Drop when no build-time or test-time file imports a Node API | These declarations track the Node runtime pinned by `.node-version` and cannot be hand-maintained | founders |

Dev-machine tools (not vendored): `just`, `cargo-deny`, Node, and `pnpm` via
corepack — Node, pnpm, and Rust are exact-pinned by `.node-version`,
`package.json#packageManager`, and `rust-toolchain.toml`; hosted workflows pin
`just` 1.47.1; cargo-deny is selected by its CI action and enforced through the
checked policy file.

The pnpm install-script allowlist contains only `esbuild`: Vite needs its
platform binary, and esbuild's installer verifies/selects that package. pnpm 11
fails installation if any other direct or transitive dependency introduces an
unreviewed build script.
