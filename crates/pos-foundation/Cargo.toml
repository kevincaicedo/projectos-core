[package]
name = "pos-foundation"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[lints]
workspace = true

[dependencies]
# Ids and event payloads serialize with stable byte-level shapes (event bodies
# are versioned serde structs, STYLE); serde is the one serialization seam.
serde = { workspace = true }
# The one crate in the workspace that may name `tracing` (m0-s15): the typed
# telemetry facade is the only emission point, which is what keeps the span
# vocabulary closed. `check-discipline` enforces the exclusivity.
tracing = { workspace = true }
