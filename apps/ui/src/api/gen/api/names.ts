// @generated by `cargo run -p pos-api --bin export-api-types`; do not edit.

export const API_SURFACE_VERSION = 12;

export const QUERY_NAMES = [
  "capability.snapshot",
  "project.inspect",
  "project.verify",
  "project.list",
  "job.list",
  "cron.preview",
  "cost.rollup",
  "health",
  "evidence.list",
  "source.health",
  "transcript.get",
] as const;
export type ApiQueryName = (typeof QUERY_NAMES)[number];

export const COMMAND_NAMES = [
  "project.create",
  "project.export",
  "project.seed-synthetic",
  "project.open",
  "project.close",
  "run.start",
  "run.cancel",
  "run.pause",
  "run.resume",
  "models.pull",
  "ingest.submit",
  "ingest.reprocess",
  "transcript.correct",
  "transcript.speaker-name",
  "transcript.speaker-assign",
] as const;
export type ApiCommandName = (typeof COMMAND_NAMES)[number];

export const STREAM_NAMES = [
  "run.steps",
] as const;
export type ApiStreamName = (typeof STREAM_NAMES)[number];

// SSE stream framing constants (pos-api stream.rs).
export const STREAM_RESUME_WINDOW_LEN = 256;
export const SSE_RETRY_MS = 2000;
