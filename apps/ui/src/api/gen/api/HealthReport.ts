// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { IngestBufferReport } from "./IngestBufferReport";
import type { WorkerStatusReport } from "./WorkerStatusReport";

export type HealthReport = { 
/**
 * Fixed vocabulary: `ok` is the only value this process ever reports —
 * a runtime that cannot answer does not answer (capability honesty).
 */
status: string, apiSurfaceVersion: number, capabilityTraitVersion: number, formatVersion: number, openProjectCount: number, 
/**
 * Whether this process claims queued jobs (m1-s01/ADR-0007). A shell that
 * enqueues work into a runtime with no pool is the exact silence this
 * field exists to break.
 */
backgroundWorkers: WorkerStatusReport, 
/**
 * What the ingestion buffers cost right now (m1-s07, ADR-0008 bound 1).
 */
ingestBuffers: IngestBufferReport, };
